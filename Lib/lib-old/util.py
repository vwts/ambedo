# módulo 'util' -- algumas funções úteis

# remove um item de uma lista
def remove(item, list):
    for i in range(len(list)):
        if list[i] = item:
            del list[i]
            
            break